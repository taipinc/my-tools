---
import Layout from '../../layouts/Layout.astro';
import { findTool } from '../../data/tools';
import ExposureBucketTool from '../../tools/exposure-bucket/index.jsx';

const { tool, category } = findTool('exposure-bucket')!;
---

<Layout title={tool.name} activeSlug={tool.slug}>
  <div class="flex flex-col h-screen">
    <div class="px-10 pt-6 pb-3 shrink-0">
      <nav class="text-[11px] uppercase tracking-[0.18em] text-[var(--color-ink-soft)] mb-2">
        <a href="/" class="hover:text-[var(--color-ink)]">Home</a>
        <span class="mx-2 opacity-60">/</span>
        <span>{category.name}</span>
        <span class="mx-2 opacity-60">/</span>
        <span class="text-[var(--color-ink)]">{tool.name}</span>
      </nav>
      <div class="flex items-baseline justify-between">
        <h1 class="text-xl font-semibold tracking-tight">{tool.name}</h1>
        <p class="text-[12px] text-[var(--color-ink-soft)]">{tool.blurb}</p>
      </div>
    </div>

    <div class="flex-1 min-h-0 mx-10 mb-6 rounded-lg border border-[var(--color-rule)] bg-[#f5f5f5] overflow-hidden">
      <ExposureBucketTool client:only="react" />
    </div>
  </div>
</Layout>
